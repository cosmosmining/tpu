# TensorTile Python dependencies (REQUIRED tier — pip-installable).
# Heavy EDA tools (yosys, sby, verible, openroad/librelane, fault) are NOT here;
# they are provisioned via apt / release binaries / CI — see DECISIONS.md.

numpy>=1.26          # bit-exact golden model arithmetic
cocotb>=1.9          # RTL <-> model lockstep benches
cocotb-bus>=0.2      # APB/SPI bus driver helpers for DV
pytest>=8.0          # model unit tests + cocotb test runner
scikit-learn>=1.3    # offline 8x8 MNIST (load_digits) train/quant + test-set split
